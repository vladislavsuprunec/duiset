interface Employee {
  salary: number;
}

const employee: Employee = {
  salary: 50000, // Assuming a default salary value
};

// Accessing the salary property
const salaryValue: number = employee.salary;
